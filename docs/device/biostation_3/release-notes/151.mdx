---
id: 151
title: 펌웨어 버전 1.5.1
---

# 펌웨어 버전 1.5.1 (빌드 번호 1.5.1_260918)

릴리즈<Separator/>2026-09-22

:::caution

- 펌웨어가 v1.4.0인 제품을 사용하는 경우, 반드시 v1.4.1로 업그레이드한 후 사용하세요.<br/>중요한 보안 및 안정성 개선이 포함되어 있으므로, 반드시 최신 버전으로 업그레이드한 후 사용해야 합니다.

- 펌웨어를 v1.4.0 이상으로 업그레이드한 뒤 하위 버전으로 다운그레이드할 수 없습니다.

- 장치의 펌웨어 다운그레이드 가능 여부 및 신규 BLE 칩 적용 여부는 장치의 시리얼 번호를 확인한 뒤 [슈프리마 홈페이지](https://supremainc.com)에 문의하세요.

:::

## 버그 수정

---

<BugLists>

    {/* <Jira num='BS3FW-2506' /> [LockOverride - User Status:Off] wiegand 리더로 도어 생성 후, wiegand 장치가 disabled 상태일 때 Lock override 카드 인증이 되지 않는 현상 - 발생버전 : V1.5.0
<Jira num='BS3FW-2471' /> [Lock override] wiegand reader로 door 생성 후, wiegand 장치가 disable 상태일때, Lock override 카드 인증이 안되는 현상- 발생버전 : V1.5.0 */}
1. Wiegand 리더가 비활성 상태일 때 해당 리더로 만든 출입문에서 **비상 개방 카드** 인증이 되지 않는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2500' /> [Auth Fail Lockout] Auth Fail Lock 상태에서 Unlock 후 메뉴 진입 이후 메인화면에서 카드 입력 시 카드 인식음만 나고 더 이상 진행되지 않는 현상 - 발생버전 : V1.5.0 */}
1. **인증 실패 잠금** 해제 후 메뉴에 진입했다가 돌아오면 카드 인증이 진행되지 않는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2498' /> [Auth Fail Lockout - JobCode 오동작] JobCode 진입 후 ID+PIN으로 인증 실패 시 표시되는 팝업이 관리자 메뉴 진입 실패 팝업으로 나타나지만, 실제 동작은 Access Auth Failure Lockout인 현상 - 발생버전 : V1.5.0 */}
1. **작업 코드** 진입 후 인증 실패 시 표시되는 팝업과 실제 동작이 일치하지 않는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2497' /> [Auth Fail Lockout 오동작] BS3FW-2496 상태에서 한 번의 인증 실패로 장치가 잠기지만, BioStar에는 잠김 알림이 올라오지 않고 정상으로 표시되는 현상 - 발생버전 : V1.5.0 */}
1. **인증 실패 잠금**으로 장치가 잠겨도 BioStar X에 잠김 상태가 표시되지 않는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2496' /> [Auth Fail Lockout - Unlock 오동작] 동일 권한으로 Auth와 Menu를 동시에 잠근 상태에서 ID+PIN으로 Unlock하면 두 권한 모두 해제되는 현상 - 발생버전 :  V1.5.0 */}
1. ID + PIN으로 **인증 실패 잠금**을 해제하면 해제 대상이 아닌 잠금까지 함께 해제되는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2495' /> [Auth Fail Lockout - Unlock 오동작] AuthFailLock 상태에서 Unlock을 시도하다 PIN 입력 실패 시 관리자 실패 횟수가 증가하는 현상 - 발생버전 :  V1.5.0 */}
1. **인증 실패 잠금** 해제 중 PIN 입력에 실패하면 관리자 인증 실패 횟수가 차감되는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2489' /> [Auth Fail Lockout - Slave 관련 오동작] AuthFailLock 상태인 Slave 장치를 삭제 후 재추가하면 AuthFailLock 설정이 기본으로 적용되어 PIN 한 번 실패만으로 즉시 Auth FailLock으로 잠기는 현상 - 발생 버전: V1.5.0 */}
1. **인증 실패 잠금** 상태의 슬레이브 장치를 삭제 후 재추가하면 한 번의 인증 실패로 바로 잠기는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2485' /> [Auth Fail Lock Out - Door Control] Auth FailLock 동작 상태에서 Door Control에 대해서 Unlock, Lock 실행 시 Administrator authentication failed 이벤트가 발생하는 현상 - 발생버전 : V1.5.0 */}
1. **인증 실패 잠금** 상태에서 출입문을 제어하면 관리자 인증 실패 이벤트가 발생하는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2476' /> [Auth Fail Lockout-2차 잠금 Off] 2차 Admin AuthFailLockOut 잠금 상태에서  2차 잠금을 Off 시 1차 잠금으로 동작이 안되고, 2차 잠금 상태를 유지 하는 현상 - 발생버전 : V1.5.0 */}
1. 2차 **인증 실패 잠금** 상태에서 **2차 잠금 시간**을 끄면 1차 잠금으로 전환되지 않고 2차 잠금이 유지되는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2472' /> AuthFailLock 상태에서 Unlock 시 ID+PIN으로 인증 성공이 되면서 AuthFailLock이 해제되는 현상 - 발생버전 : V1.5.0 */}
1. **인증 실패 잠금** 해제 시 인증이 성공 처리되며 잠금이 해제되는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2470' /> 사용자에 Lock Override 카드가 설정되어 있으면 Trigger&Action으로 하위 Wiegand Reader가 Disable Device동작 안되는 문제 - 발생버전 : V1.4.0 */}
1. 사용자에게 **비상 개방 카드**가 설정되어 있으면 **작업 조건 및 동작**으로 하위 Wiegand 리더가 장치 잠금되지 않는 문제 <Badge>1.4.0</Badge>

    {/* <Jira num='BS3FW-2469' /> [FISF-2487] osdp_keyset로 들어온 키로 동작하지 않고, default key로 동작하는 문제 - 발생버전 : V1.5.0 */}
1. OSDP 보안 통신에서 새로 설정한 키가 적용되지 않고 기본 키로 동작하는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2467' /> [Auth Fail Lockout Release] 지원(M)-지원(S)일 때 Slave Auth FailLock Out 상태에서 Master 재부팅 후 1회만 Slave AuthFailLockOut 정상적으로 해제되고, 이후 발생하는 Auth FailLock에 대해서 오동작하는 현상 - 발생버전 : V1.5.0 */}
1. 마스터 재부팅 후 슬레이브의 **인증 실패 잠금**이 한 번만 정상 해제되고 이후 오동작하는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2466' /> [Auth Fail Lockout-Slave오동작] Arm 인증 시도 시 PIN에 대해 실패 시에 Auth Fail Lockout으로 동작하지 않고 Access Denied로 동작 - 발생버전 : V1.5.0 */}
1. 경비(Arm) 인증 시 PIN 실패가 **인증 실패 잠금**으로 처리되지 않고 출입 거부로 동작하는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2465' /> [Auth Fail Lockout] 지원(M)-지원(S)일 때 지원 Slave의 AuthFailLockout을 Card 인증으로 해제 할 수 없는 현상 - 발생버전 : V1.5.0 */}
1. 슬레이브의 **인증 실패 잠금**을 카드 인증으로 해제할 수 없는 문제 <Badge>1.5.0</Badge>

    {/* <Jira num='BS3FW-2461' /> [BSF-3178] BS2_UpdateResource 및 BS2_SetDisplayConfig 호출 후 로고 이미지가 즉시 반영되지 않는 현상 - 발생버전 : V1.0.0 */}
1. SDK를 이용하여 홈화면 로고 이미지를 변경하면 재부팅 전까지 화면에 반영되지 않는 문제 <Badge>1.0.0</Badge>

</BugLists>
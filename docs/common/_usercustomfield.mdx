<Head level={3} hashid={props.hashid}><Cmd sid='settings.activeDirectory.FieldConfiguration' product='2' /></Head>

**BioStar X**의 사용자 필드와 맵핑할 <strong>{props.name}</strong> 필드를 설정할 수 있습니다. <Cmd sid='settings.activeDirectory.FieldConfiguration' product='2' /> 섹션에서 **BioStar X**의 사용자 필드로 사용할 <strong>{props.name}</strong>의 필드를 선택하세요.

<Image src={`/img/${props.img01}`} />

:::info

**BioStar X**의 각 사용자 필드는 <strong>{props.name}</strong>의 사용자 정보와 일치하는 항목을 맵핑할 수 있도록 기본 설정되어 있습니다. 기본 설정된 필드값이 아닌 다른 필드값을 선택하려면 <Cmd sid={props.sid} product='2' /> 열의 항목을 클릭하고 원하는 필드값을 선택하세요.

<Image src={`/img/${props.img02}`} />

:::
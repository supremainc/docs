<Head level={3} hashid={props.hashid}>
  <Cmd sid="settings.activeDirectory.FieldConfiguration" product="2" />
</Head>

Configura el campo de <strong>{props.name}</strong> para asignarlo a los campos de usuario de **BioStar X**. Selecciona el campo de <strong>{props.name}</strong> que se usará como campo de usuario de **BioStar X** en la sección <Cmd sid="settings.activeDirectory.FieldConfiguration" product="2" />.

<Image src={`/img/${props.img01}`} />

:::info

De forma predeterminada, cada campo de usuario de **BioStar X** se asigna a los elementos que coinciden con la información de usuario de <strong>{props.name}</strong>. Haz clic en el campo en <Cmd sid={props.sid} product="2" /> para seleccionar un valor de campo diferente al predeterminado.

<Image src={`/img/${props.img02}`} />

:::

---
id: enroll-credential-mobile
title: Enroll Mobile Access Cards
description: Integrating with Suprema's Airfob Portal enables issuing mobile access cards to users.
keywords:
  - Mobile access card
  - Enrollment
  - Credentials
  - Certifications
  - card
---

Integrating with Suprema's Airfob Portal enables issuing mobile access cards to users. Users can enroll mobile access cards individually or use the CSV import feature to enroll multiple users at once.

Depending on the mobile access card distribution method set in the Airfob Portal, user emails or phone numbers must be entered.

:::info

* Only one of the **CSN Mobile Card** or the **Template on Mobile** can be used.

* This feature can only be used when linked with the Airfob portal. For more information regarding the Airfob Portal and mobile access card use, refer to the [following](settings-credential-mobile-access-card).

:::

## <Cmd sid='team.user.csn' className='normal' /> {#csnmobilecard}

You can issue CSN mobile access cards to users.

### Card assignment

1. In the <Cmd sid='common.credential' product='2' /> section, click the <Cmd sid='common.mobileCredential' product='2' /> button.

2. When the <Cmd sid='user.header.mobile' product='2' /> window appears, select <Cmd sid='user.cardType' product='2' /> option and select <Cmd sid='user.cardEnrol.cardTypeCsnMobile' product='2' />.

3. In the <Cmd sid='common.registrationOption' product='2' /> options, select <Cmd sid='common.assignCard' product='2' />.

   <Image src='/img/biostarx-new-user-credential-mobile-csn-assign.png' />

4. Select or search for the card desired to assign from the card list.

5. Set the <Cmd sid='common.info' product='2' /> and <Cmd sid='common.validPeriod' product='2' />.

6. Click the <Cmd sid='button.enroll' product='2' /> button.

:::info

* If you enabled photo, department, title options in the <Cmd sid='common.info' product='2' /> section, this information can be displayed on the user's mobile access card.

* The <Cmd sid='common.validPeriod' product='2' /> field is activated when the site is created as a **Dynamic** site in the Airfob Portal. You can set the expiration duration and usage period for the mobile access cards. For more information about the Airfob Portal, refer to the [following link](https://airfob.zohodesk.com/portal).

:::

### Enter manually

1. In the <Cmd sid='common.credential' product='2' /> section, click the <Cmd sid='common.mobileCredential' product='2' /> button.

2. When the <Cmd sid='user.header.mobile' product='2' /> window appears, select <Cmd sid='user.cardType' product='2' /> option and select <Cmd sid='user.cardEnrol.cardTypeCsnMobile' product='2' />.

3. Select <Cmd sid='common.enterManually' product='2' /> from the <Cmd sid='common.registrationOption' product='2' /> options.

   <Image src='/img/biostarx-new-user-credential-mobile-csn.png' />

4. Select <Cmd sid='zone.intrusion.inputType' product='2' />.

   * <Cmd sid='user.random.cardid' product='2' />: Automatically generate a card ID.

   * <Cmd sid='common.useUserId' product='2' />: Uses the user ID as the card ID.

   * <Cmd sid='user.manual.input' product='2' />: Allows manual entry of the card ID.

5. Set the <Cmd sid='common.info' product='2' /> and <Cmd sid='common.validPeriod' product='2' />.

6. Click the <Cmd sid='button.enroll' product='2' /> button.

:::info

* If you enabled photo, department, title options in the <Cmd sid='common.info' product='2' /> section, this information can be displayed on the user's mobile access card.

* The <Cmd sid='common.validPeriod' product='2' /> field is activated when the site is created as a **Dynamic** site in the Airfob Portal. You can set the expiration duration and usage period for the mobile access cards. For more information about the Airfob Portal, refer to the [following link](https://airfob.zohodesk.com/portal).

* If the user loses or deletes the activation code sent via email or text message and needs to reissue the activation code, click the **Resend** button.

* For more information about the CSN mobile application, refer to the [following link](https://www.csn.edu/csn-mobile-apps).

:::

## <Cmd sid='team.user.tom' className='normal' /> {#template-on-mobile}

**Template on Mobile** is a mobile access card that stores a user's biometric template on a mobile device. Authentication can occur biometrically without storing user data on the **BioStar X** server, Airfob Portal or devices.

This functionality is particularly useful in environments where biometric data cannot be stored on servers or devices due to privacy concerns. The issuing of the **Template on Mobile** and the biometric template enrollment process occurs separately, allowing users to enroll their visual face directly on **Template on Mobile**-supported devices without interacting with an administrator.

1. In the <Cmd sid='common.credential' product='2' /> section, click the <Cmd sid='common.mobileCredential' product='2' /> button.

2. When the <Cmd sid='user.header.mobile' product='2' /> window appears, select <Cmd sid='user.cardType' product='2' /> option and select <Cmd sid='user.cardEnroll.cardTypeTemplateOnMobile' product='2' />.

   <Image src='/img/biostarx-new-user-credential-mobile-tom.png' />

3. Select the desired card type from <Cmd sid='user.cardModeLong' product='2' />.

   * <Cmd sid='user.accessOnCard' product='2' />: User information (card ID, PIN, access group, duration, private authentication mode) can be stored on the card.

   * <Cmd sid='user.secureCredentialCard' product='2' />: Secure credential card. User card ID and PIN data can be stored. **Server Matching** needs to be enabled for user information stored in **BioStar X**.

4. Set the <Cmd sid='common.info' product='2' /> and <Cmd sid='common.validPeriod' product='2' />.

5. Click the <Cmd sid='button.enroll' product='2' /> button.

An issuance email will be sent to the registered user's email address. The user must install the Airfob Pass application through the link in the email to complete the issuance of the **Template on Mobile**.

:::tip

**How to Template on Mobile Authentication**

The user tags the mobile device that issued the **Template on Mobile** against the auth device and follows the on-screen prompts to authenticate their face.

:::

:::info

* If you enabled photo, department, title options in the <Cmd sid='common.info' product='2' /> section, this information can be displayed on the user's mobile access card.

* The <Cmd sid='common.validPeriod' product='2' /> field is activated when the site is created as a **Dynamic** site in the Airfob Portal. You can set the expiration duration and usage period for the mobile access cards. For more information about the Airfob Portal, refer to the [following link](https://airfob.zohodesk.com/portal).

* For devices and firmware versions that support **Template on Mobile**, refer to the list below.

  * BioStation 3 firmware version 1.2.0 or higher

  * BioEntry W3 firmware version 1.0.0 or higher

* Only one of the **CSN Mobile Card** or the **Template on Mobile** can be used.

* Only one **Template on Mobile** can be issued per user.

:::

---
id: settings-advanced-ac-intrusion-alarm
title: Intrusion Alarm
description: This guides you on how to set up security. The intrusion alarm feature provides capabilities to lock doors or trigger alarms when intrusion is detected.
keywords:
  - Advanced access control
  - Intrusion Alarm
---

This guides you on how to set up security. The intrusion alarm settings provide capabilities to lock doors or trigger alarms when intrusion is detected.

:::info

<Glossary termid='intrusion_alarm_zone' />

:::

## Add intrusion alarm {#addIntrusionAlarmZone}

1. Click <Cmd sid='shell.header.setting' /> on the <Cmd sid='shell.header.launcher' /> page.

2. Click <Cmd sid='setting.menu.advancedAc.root' /> in the left sidebar.

3. Click <Cmd sid='button.func.addZone' product='2' />.

4. Select <Cmd sid='zone.intrusion' product='2' /> and click <Cmd sid='button.apply' product='2' />.

5. Enter the name of the intrusion alarm setting to add in the <Cmd sid='common.info' product='2' /> section.

   <Image src='/img/biostarx-settings-adv-ac-add-intrusion.png' />

6. Set the details of the intrusion alarm settings in the <Cmd sid='common.setting' product='2' /> section. For more information, refer to [the following](#setting).

7. Add authentication settings for arming and disarming in the <Cmd sid='zone.intrusion.armConfiguration' product='2' /> section. For more information, refer to [the following](#armConfiguration).

   <Image src='/img/biostarx-settings-adv-ac-add-intrusion-set-arm.png' />

   :::note

   The <Cmd sid='zone.intrusion.armConfiguration' product='2' /> section appears when you select a door in the <Cmd sid='common.info' product='2' /> section.

   :::

8. Set up intrusion detection signals in the <Cmd sid='zone.intrusion.intrusionSetting' product='2' /> section. For more information, refer to [the following](#intrusionSetting).

   <Image src='/img/biostarx-settings-adv-ac-add-intrusion-set-intrusion.png' />

9. Set the actions to be performed when specific events occur in the <Cmd sid='common.alarm' product='2' /> section.

10. After completing all settings, click <Cmd sid='button.apply' product='2' /> at the bottom of the screen.

:::info

The <Cmd sid='zone.intrusion.armConfiguration' product='2' />, <Cmd sid='zone.intrusion.intrusionSetting' product='2' />, and <Cmd sid='common.alarm' product='2' /> sections are available only if the <Cmd sid='zone.pause.doorselected' product='2' /> option is set in the <Cmd sid='common.setting' product='2' /> section.

:::

## Setting options guide {#setting}

The details for intrusion alarm settings are as follows.

* <Cmd sid='zone.globalZone' product='2' />: Set the application range. The intrusion alarm settings only support the <Cmd sid='zone.globalZone.local' product='2' /> mode and can only be set with devices connected via RS-485 and entry devices.

* <Cmd sid='zone.pause.activatedAndDeactivated' product='2' />: You can activate or temporarily deactivate the settings.

* Select the door to apply the intrusion alarm function at <Cmd sid='zone.pause.doorselected' product='2' />.

## Arming or disarming settings {#armConfiguration}

You can add authentication settings for arming and disarming.

<Image src='/img/biostarx-settings-adv-ac-add-intrusion-set-arm.png' />

* Set the delay time until arming or disarming with <Cmd sid='zone.intrusion.delayTime' product='2' />. This means that the arming is the delay time after authentication until the alarm starts, and the disarming is the delay time before the alarm is triggered after an intrusion is detected.

* Add cards authorized for arming or disarming with <Cmd sid='zone.intrusion.accessCard' product='2' />. You can register up to 128 arming/disarming cards.

* Select groups authorized for arming or disarming with <Cmd sid='zone.intrusion.accessGroup' product='2' />. You can register up to 128 arming/disarming groups.

* Set the arming/disarming by the device or input signal with <Cmd sid='zone.intrusion.armInput' product='2' />. Click the <Cmd sid='button.plusAdd' product='2' /> button and configure each item.

### Add arming/disarming by device {#addArmInput}

Click the <Cmd sid='button.plusAdd' product='2' /> button in the <Cmd sid='zone.intrusion.armInput' product='2' /> (<Cmd sid='common.device' product='2' />) option. Select the device to control the intrusion alarm feature and choose <Cmd sid='zone.intrusion.armType' product='2' />.

<Cmd sid='zone.intrusion.inputType' product='2' /> can select <Cmd sid='zone.intrusion.inputType.card' product='2' />, <Cmd sid='zone.intrusion.inputType.key' product='2' />, or <Cmd sid='zone.intrusion.inputType.cardOrKey' product='2' />. Devices without an LCD screen can only use <Cmd sid='zone.intrusion.inputType.card' product='2' /> for input type.

<Image src='/img/biostarx-settings-advanced-ac-intusion-add-device.png' />

### Add arming/disarming by input signal {#addArmInputBySignal}

Click the <Cmd sid='button.plusAdd' product='2' /> button in the <Cmd sid='zone.intrusion.armInput' product='2' /> (<Cmd sid='zone.intrusion.inputType.input' product='2' />) option. Select the device to control the intrusion alarm feature. Click <Cmd sid='zone.intrusion.inputType.port' product='2' /> to choose the input port of the selected device.

After choosing <Cmd sid='zone.intrusion.armType' product='2' />, set the type of switch and the duration of the signal.

<Image src='/img/biostarx-settings-advanced-ac-intusion-add-signal.png' />

## Intrusion detection settings {#intrusionSetting}

You can set up the intrusion detection signals. Click <Cmd sid='button.addplus' product='2' /> and set it as shown below to recognize an intrusion when an N/O sensor connected to input port 0 of the BioStation 3 device sends a signal for 100(ms).

<Image src='/img/biostarx-settings-advanced-ac-intusion-detect-settings.png' />

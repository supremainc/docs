---
id: 231
title: Firmware Version 2.3.1
---

# Firmware Version 2.3.1 (Build No. 2.3.1_260922)

Release<Separator/>2026-09-22

:::caution

For devices with the new SE processor, after upgrading the firmware to v2.1.0 or higher, you cannot downgrade to a lower version and downgrade the firmware customized in the lower version.

Devices with existing SE processor can be downgraded to a lower version.

For more information, check the serial number of the device and contact the [Suprema](https://supremainc.com).

:::

## Bug Fixes

---

<BugLists>

    {/* (DFW-1390) [Lock override-User Status:Off] wiegand reader로 door 생성 후, wiegand 장치가 disable 상태일때, Lock override 카드 인증이 안되는 현상
(DFW-1382) [Lock override] wiegand reader로 door 생성 후, wiegand 장치가 disable 상태일때, Lock override 카드 인증이 안되는 현상 */}
1. **Lock Override** card authentication not working on a door created with a Wiegand reader when the reader is disabled. <Badge>2.3.0</Badge>

    {/* (DFW-1388) timed apb 설정 후 toggle 진입 시 APB 상태 갱신이 안되는 현상 */}
1. **Anti-passback** status not updating after door control through authentication in a **Timed Anti-passback** environment. <Badge>2.3.0</Badge>

    {/* (DFW-1384) wiegand reader로 door가 생성되어 있으면, Trigger&Action으로 하위 Wiegand Reader가 Disable Device 동작 안되는 문제 */}
1. Child Wiegand reader not being locked by **Trigger & Action** when a door is created with a Wiegand reader. <Badge>2.3.0</Badge>

    {/* (DFW-1381) trigger&Action 및 구역 알람 액션에 Relay 설정된 경우 릴레이 발생 횟수만큼 로그가 남는 문제 */}
1. Logs recorded multiple times when a relay is configured in **Trigger & Action** or **Zone** alarm. <Badge>2.3.0</Badge>

    {/* (DFW-1394) Toggle 진입 후 SLAVE 재부팅 시 SLAVE 장치에 자물쇠 사라지는 현상 */}
1. Lock icon disappearing when a slave device is rebooted while the door is manually unlocked. <Badge>2.3.0</Badge>

</BugLists>
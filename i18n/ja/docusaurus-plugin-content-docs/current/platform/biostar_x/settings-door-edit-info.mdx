---
id: settings-door-edit-info
title: 出入口情報を編集
description: 登録済みの出入口の設定情報を編集する方法を説明します。
keywords:
  - ドア
  - 情報編集
---

登録済みの出入口の設定情報を編集する方法を説明します。 個別の出入口の詳細設定を変更するか、複数の出入口を選択して共通項目を一括編集できます。

## 1つの出入口情報を編集 {#edit-one-door-info}

1. <Cmd sid='shell.header.launcher' />ページで<Cmd sid='shell.header.setting' />をクリックします。

2. 画面左側のサイドバーで <Cmd sid='setting.menu.door.root' /> をクリックします。

3. 出入口リストで編集する出入口をクリックします。

4. 各セクションの詳細項目を編集します。

5. すべての設定が完了したら、画面最下部の <Cmd sid='button.apply' product='2' /> ボタンをクリックします。

:::info

各セクションの詳細については[次のドキュメント](settings-door-add)を参照してください。

:::

## 複数の出入口を一括編集 {#edit-multiple-door-info}

1. <Cmd sid='shell.header.launcher' />ページで<Cmd sid='shell.header.setting' />をクリックします。

2. 画面左側のサイドバーで <Cmd sid='setting.menu.door.root' /> をクリックします。

3. 出入口リストで編集する出入口のチェックボックスをクリックします。 2つ以上の出入口を選択します。

4. 画面右上にある<Cmd sid='button.batchEdit' product='2' />をクリックします。

5. <Cmd sid='button.batchEdit' product='2' />ウィンドウが表示されたら、必要なオプションを設定します。

   <Image src='/img/biostarx-setting-door-edit-batch.png' />

   * <Cmd sid='door.openTime' product='2' />：認証完了後に出入口が開いたままになる時間を設定します。 この時間を過ぎると、出入口は自動的にロックされます。

6. 設定が完了したら<Cmd sid='button.apply' product='2' />をクリックします。

:::info

<Cmd sid='button.batchEdit' product='2' />は、出入口リストで2つ以上の出入口を選択すると有効になります。

:::
